import popUp from './popup';
export { popUp };

import getComponentRootDom from './getComponentDom';
export { getComponentRootDom };

import loading from './loading';
export { loading };

import myFetch from './fetchData';
export { myFetch };

import formatDate from './formatDate';
export { formatDate };

import debounce from './debounce';
export { debounce };

import lazy from './lazy';
export { lazy };

import toTop from './toTop';
export { toTop };

export default {
  popUp,
  getComponentRootDom,
  loading,
  myFetch,
  formatDate,
  debounce,
  lazy,
  toTop
};
